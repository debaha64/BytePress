# Tools

`Tools/` хранит детерминированные инструменты `BytePress`.

## Назначение
- выполнять воспроизводимые операции поверх утверждённых договоров;
- материализовать начальный продуктовый слой;
- запускать структурные проверки и локальный smoke-маршрут продукта;
- в целевой модели materialize локальный `Tools/*` продукта, чтобы продукт был независимым после создания;
- не подменять документы-владельцы и инженерное решение.

## Границы
`Tools/*` не владеет проверочным контуром целиком. Смысл проверок, уровни, доказательства и подтверждение результата живут в `Docs/Technical/Verification.md`. Инструмент исполняет действие, но не закрывает `Roadmap`, `Backlog`, `Plan`, процессный гейт или итоговую интерпретацию.

## Карта артефактов
- `bp_lint.py` — структурная проверка `BytePress`, свежего продукта и развивающегося продукта.
- `bp_check.py` — проверка связности контуров уровня `check` по минимальному deterministic-составу.
- `bp_bootstrap.py` — материализация первого пригодного к работе продуктового репозитория по договору `BytePress`.
- `bp_integration_smoke.py` — детерминированный локальный smoke-маршрут продукта без внешних подключений.
- `bp_normalize_terms.py` — нормализация карточек терминов и пересборка индекса.
- `README.md` — карта инструментального домена.

## Режимы `bp_lint.py`
- `--repo .` — структурная проверка самого `BytePress`.
- `--mode product-fresh` — строгая проверка состояния сразу после `bp_bootstrap.py`.
- `--mode product-developed` — проверка продукта после подтверждения текущей истины и закрытия первого прохода.
- `--mode auto` — режим проверки generated product repo; выбор fresh/developed идёт по статусу текущей истины.

`bp_lint.py` остаётся структурной проверкой. Он подсвечивает противоречия жизненного цикла, но не оценивает предметное качество продукта.

Проверочные маркеры в документах могут быть русскими. `bp_lint.py` принимает русские эквиваленты для стартового отчёта, аналитического гейта, текущей истины, документов-владельцев и записываемых действий, сохраняя прежние структурные условия.

`bp_lint.py` и создаваемый `Tools/product_check.py` также ловят:
- отсутствие правила ожидания ответа после блокирующего интервью;
- самовольное подтверждение текущей истины общим запросом пользователя;
- самовольный выбор `tkinter`;
- попытку установить системный пакет в пользовательских текстах создаваемого слоя;
- расширяющие функции первого прохода без явного ответа пользователя;
- возврат `GUI`, `product pass`, `pass` и `bootstrap` в пользовательские тексты создаваемого слоя там, где есть русский эквивалент.

## Product-local tools
`bp_bootstrap.py` materialize generated product repo с локальным `Tools/*`:
- `Tools/product_check.py` — основной structural check продукта без `BYTEPRESS_ROOT`;
- `Tools/product_bootstrap_smoke.py` — локальный smoke route с отчётом в `Tools/.reports/`;
- `Tools/*` является единственным служебным входом нового каркаса.

`scripts/*` — наследие старых уже созданных продуктов. Новый каркас его не создаёт; миграция старых продуктов описана в `Docs/Technical/Product_Service_Update_Route.md`.

## Периметр `bp_check.py`
- `bp_check.py` является инструментом уровня `check` для deterministic-проверок связности контуров.
- Базовый запуск: `python3 Tools/bp_check.py --repo .`.
- Поддерживаемые форматы вывода: `--format text` и `--format json`.
- `bp_check.py` не заменяет `bp_lint.py`: `bp_lint.py` остаётся быстрой структурной проверкой уровня `lint`, а `bp_check.py` не дублирует его назначение и не расширяет его код.
- Вывод `bp_check.py` короткий, машинно понятный и пригодный для отчёта агента; провал фиксируется как риск или блокер по уровню нарушения.
- Спорные случаи остаются за агентом и владельцем: инструмент не оценивает смысл, не принимает решение о merge и не закрывает `ROAD/BACK/PLAN`.
- Минимальный текущий состав проверок: прямые локальные Markdown-ссылки, валидность JSON-схем в `Schemas/*`, базовая форма карточек `Docs/Terms/TERM-*.md`, допустимые статусы терминов, допустимые решения по конфликту и отсутствие явных дублей ID в базовых индексах `Roadmap`, `Backlog`, `ChangeLog`, `QualityLog`, `Base_Terms`.
- Будущее расширение уровня `check`: согласованность цепочки `ROAD/BACK/PLAN`, журнальных связей, архивных структур, README-карт и терминологических запретов. Эти проверки не включены в минимальный инструмент, чтобы не создавать ложные срабатывания без отдельного договора реализации.
- `bp_verify` может собирать пакет доказательств только после отдельного договора.
- tooling для подтверждения результата допустим только как поддержка упаковки и связей, без автоматического verdict.

## Куда идти дальше
- технический договор bootstrap: `../Docs/Technical/Product_Bootstrap_Contract.md`;
- проверочный контур: `../Docs/Technical/Verification.md`;
- подтверждение результата: `../Docs/Technical/Verification.md`;
- жизненный цикл артефактов: `../Docs/Technical/Artifact_Lifecycle.md`.
